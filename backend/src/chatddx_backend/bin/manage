#!/usr/bin/env bash
# manage.sh

case "$1" in
runserver)
  "$DJANGO_APP/bin/gunicorn" chatddx_backend.wsgi:application "${@:2}"
  ;;
static)
  echo "$DJANGO_STATIC"
  ;;
*)
  cd "$DJANGO_APP" && ./bin/django-admin "${@}"
  ;;
esac
